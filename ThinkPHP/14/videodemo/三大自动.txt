1，自动验证  （他的生效时期在ACTION控制器里面的生效时机是create方法是生效）。
主要是写要自定义模型里面，来完成用户输入信息的时候对表单或数据库当中的字段来进行验证

2，自动完成（自动填充）  就是当用户输入的字段，并不是需要用户手动添写的，这些自动，而是由系统
自动生成的这些字段，我们在这种情况下使用自动完成。（密码，注册，注册IP，做为自动完成，注册时间）

3，字段映射（自动映射）	如果说我将表单当中的字段改为与数据库当中的字段不一样？会怎么样？

记忆最深刻的是自己的实验结果。  如果不一致，那是插入数据是不会成功。（可空字段除外）

如果是数据库当中的字段与表单当中的字段一致的话，就会产生问题。

那你这当中的数据库字段，就可以被用户给猜出来。这种情况一就不安全？在此种情况下，我们使用自动映射



我在上次写的用户注册当中来完成三大自动。




取名为三大自动。